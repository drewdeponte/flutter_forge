name: flutter_forge
description: >
  A robust opinionated framework for building state based applications in a
  modular, testable, and composable style.
version: 0.8.1
homepage: https://github.com/uptech/flutter_forge
repository: https://github.com/uptech/flutter_forge

environment:
  sdk: '>=2.18.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
